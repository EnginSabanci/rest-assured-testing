package test1;

import org.junit.jupiter.api.Test;
import static io.restassured.RestAssured.*;
import static org.junit.jupiter.api.Assertions.*;
public class ORDSTests {
    //RestAssure is pretty much new class in RestAssured library. We call web service, specify parameters.
    //We don't have to use class if we import them
    @Test
    public void test1 (){

    }
}
